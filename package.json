{
  "name": "planningpoker",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "concurrently -n server,web -c magenta,cyan \"npm -w @pp/server run dev\" \"npm -w @pp/web run dev\"",
    "build": "npm -w @pp/web run build",
    "start": "npm -w @pp/server run start"
  },
  "devDependencies": {
    "concurrently": "^9.2.0"
  }
}
